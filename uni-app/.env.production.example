# 复制为 .env.production，用于 npm run build:mp-weixin 等生产构建
#
# 微信小程序必须使用完整 HTTPS 地址（不能使用 /api 相对路径）
# 与微信公众平台 → 开发设置 → 服务器域名 中的 request 域名一致（仅主机名，不带路径）

VITE_API_BASE_URL=https://frontend.duimai.net/api

# 可选：人工回复 WebSocket（不配则从 VITE_API_BASE_URL 自动推导 wss）
# 微信后台需单独配置 socket 合法域名（wss://runner.example.com）
# VITE_RUNNER_WS_BASE=wss://runner.example.com/api

# 可选：iconfont.cn 项目「Font class」在线 ttf 地址（https），不配则用内置 base64 子集字体
# 需在 downloadFile 合法域名加入 at.alicdn.com 或你的 CDN
# VITE_ICONFONT_URL=https://at.alicdn.com/t/font_xxxx.ttf
