# npm run dev:mp-weixin 使用本文件（编译到 dist/dev/mp-weixin）
#
# 必须为完整 http(s) 地址；不可写 /api（H5 专用，小程序会忽略并改用 runner-api-base.js 兜底）
# 开发者工具联调本地 8091：勾选「不校验合法域名」
# 真机预览请改为 HTTPS 域名（与 .env.production 相同）

VITE_API_BASE_URL=http://localhost:8091/api
